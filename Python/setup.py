from setuptools import setup

setup(name = 'co2_sensor',
      version = '1.0',
      desription = 'CO2 sensor interface',
      url = '-',
      author = '-',
      author_email = '-',
      licence = '-',
      packages = ['CO2'],
      zip_safe = False)
